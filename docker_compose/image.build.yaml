version: "3.8"

services:
  sar_bd_mysql_v1.0.1:
    image: ghcr.io/joseesco24/sar_bd_mysql:v1.0.1
    build:
      dockerfile: Dockerfile
      context: ../.

  sar_bd_mysql_latest:
    image: ghcr.io/joseesco24/sar_bd_mysql:latest
    build:
      dockerfile: Dockerfile
      context: ../.
